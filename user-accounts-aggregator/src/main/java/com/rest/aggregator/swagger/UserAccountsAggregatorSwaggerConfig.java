package com.rest.aggregator.swagger;

import springfox.documentation.service.Contact;

//@Configuration
//@EnableSwagger2
public class UserAccountsAggregatorSwaggerConfig {/*
	@Bean
	public Docket api() {
		return new Docket(DocumentationType.SWAGGER_2)
			.apiInfo(getApiInfo())
			.select()
			.apis(RequestHandlerSelectors
			.basePackage("com.rest.aggregator.controllers"))
			.paths(PathSelectors.any())
			.build();
	}
	
	private ApiInfo getApiInfo() {
		Contact contact = new Contact("nmore", "http://google.com", "nilesh9291.javadeveloper@gmail.com");

		return new ApiInfoBuilder()
			.title("User Aggregate Application")
			.description("REST API's for User Aggregate Application")
			.version("1.0.0")
			.license("Apache 2.0")
			.licenseUrl("http://www.apache.org/licenses/LICENSE-2.0")
			.contact(contact)
			.build();
	}
*/}
